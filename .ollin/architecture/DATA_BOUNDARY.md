# Data Boundary

Bulk scientific datasets and generated experiment runs are local/runtime artifacts, not normal repository source.

The repository stores:

- source descriptors;
- provenance manifests;
- schemas;
- download/import code;
- tiny synthetic fixtures;
- qualification evidence that is safe and appropriately sized for Git.

The repository does not store bulk FAFB/BANC connectome dumps by default.
